---
title: Multi-Agent Planning with Spatio-Temporal and Topological Constraints using STL-GO
title_zh: 基于STL-GO的时空与拓扑约束多智能体规划
authors: "Sheryl Paul, Vidisha Kudalkar, Anand Balakrishnan, Lars Lindemann, Alberto Speranzon, Jyotirmoy V. Deshmukh"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.28679v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向时空与拓扑约束的多智能体规划方法
tldr: 多智能体规划问题在工程中广泛应用，需同时满足时空（何时何地做什么）和拓扑（智能体如何交互）约束，STL-GO逻辑可表达这些需求，但路径规划求解面临编码多个时变图的挑战。本文提出基于混合整数规划（MIP）与可满足性模理论（SMT）的两种编码方案，并给出可靠性保证。同时提供统一接口来指定智能体约束、图拓扑和STL-GO规格，使两种方法无缝使用并可直接比较。通过多无人机搜救基准实验，改变团队规模和图复杂度，验证了编码在动态多图交互下的表达性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.28679v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 741, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28679v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1881, \"height\": 610, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28679v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1883, \"height\": 612, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.28679v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1757, \"height\": 622, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28679v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 715, \"height\": 525, \"label\": \"Table\"}]"
motivation: STL-GO可表达多智能体时空与拓扑约束，但规划求解需处理时变图编码的复杂性。
method: 提出基于MIP和SMT的两种编码方案，构建统一接口以同时指定智能体约束、图拓扑和STL-GO规格。
result: 在多无人机搜救基准上改变团队规模和图复杂度进行实验，验证两种编码的表达性与有效性。
conclusion: 提供有可靠性保证且可比较的规划工具，支持动态多图交互场景。
---

## 摘要
多智能体规划问题出现在多种工程应用中，如多机器人 wildfire 灭火和工厂中的无人机巡检。一个特别的挑战是存在时空（即智能体应在何时和/或何处执行什么任务）和拓扑约束（即智能体应如何交互），这些通常通过图的概念来形式化。近年来，已提出各种框架，能够通过时空逻辑捕获此类约束。我们在此关注带图算子的时空逻辑（STL-GO），这是一种近期出现的支持对多个智能体及其拓扑（如感知、通信和任务拓扑）进行推理的形式化方法。本文中，我们考虑规划满足以STL-GO编写的约束的多智能体路径的问题。由于需要通过STL-GO固有的图算子编码多个可能随时间变化的图，该问题尤其具有挑战性。我们提出了该问题的两种编码，一种基于混合整数规划（MIP），另一种基于可满足性模理论（SMT），并具有可靠性保证。我们提供了一个统一接口，用于指定智能体约束、其图拓扑以及STL-GO规范，从而能够无缝使用这两种方法并促进它们之间的直接比较。我们在一个多无人机搜索与救援基准上评估了这两种编码，对团队规模和图复杂度进行了消融，突出了所提编码在动态多图交互下的表达能力。

## Abstract
Multi-agent planning problems arise in a variety of engineering applications, such as multi-robot wildfire fighting and unmanned aerial inspection in factories. A particular challenge is the existence of spatio-temporal (i.e., when and/or where an agent should do what) and topological constraints (i.e., how agents should interact), as typically formalized via the notion of graphs. Over the last years, various frameworks have been proposed that can capture such constraints via spatio-temporal logics. We focus here on spatio-temporal logic with graph operators (STL-GO), a recent formalism that supports reasoning about multiple agents and their topologies, such as sensing, communication, and task topologies. In this paper, we consider the problem of planning multi-agent paths that satisfy constraints written in STL-GO. This problem is particularly challenging due to the need of encoding multiple, potentially time-varying graphs via the graph operators inherent to STL-GO. We present two encodings of this problem, one based on mixed-integer programming (MIP) and another based on satisfiability modulo theory (SMT), with soundness guarantees. We provide a unified interface for specifying agent constraints, their graph topologies, and the STL-GO specification, enabling seamless use of both methods and facilitating direct comparison between them. We evaluate both encodings on a multi-UAV search-and-rescue benchmark, ablating over team size and graph complexity, highlighting the expressiveness of the proposed encodings under dynamic multi- graph interactions.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多智能体系统（MAS）在工程中广泛应用（如多机器人 wildfire 扑救、工厂无人机巡检）。这类系统的任务行为不仅依赖时间事件，还依赖智能体之间的空间关系、通信关系、任务依赖等拓扑结构。传统时序逻辑（LTL、STL）难以直接表达“何时、何地、哪个智能体、与谁交互”这类复合约束。
- **背景**：已有多种时空逻辑（SSTL、SaSTL、SpaTeL、STREL、Census STL、STL-GO）可表达多智能体的时空与拓扑性质。其中，STL-GO 支持对多个时变交互图（感知图、通信图、任务图等）的同时量化与邻域基数约束，表达能力较强，但此前只用于运行时监控，未解决规划（planning）问题。
- **核心问题**：如何为满足 STL-GO 规范的多智能体系统合成开环、集中式、有界时域的控制序列（路径规划），并保证正确性。该问题的主要难点在于需要对多个可能存在且随时间变化的交互图进行编码，并处理图算子带来的量化与基数约束。
- **整体含义**：本文首次为 STL-GO 提供了 MIP 与 SMT 两种求解编码，填补了 STL-GO 从监控到规划的关键空白，为实际多智能体任务协调提供了形式化保证的规划工具。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将 STL-GO 的满足性问题转化为可求解的约束系统：
  - **MIP 编码**：利用混合整数规划，将动态、原子谓词、逻辑/时间算子、图算子、多智能体量词编码为线性/整数约束。
  - **SMT 编码**：利用可满足性模理论（LRA + LIA），将同样的语义编码为无量词的 SMT 公式。
- **系统模型与假设**：
  - 智能体同构（可扩展至角色异构），状态 \(x_t^i\)，控制输入 \(u_t^i\)，离散时间域。
  - 环境状态 \(w_t\) 确定且已知，状态转移确定性。
  - 动力学限定为仿射：\(x_{t+1}^i = A x_t^i + B u_t^i + E w_t + c\)。
  - 存在多个交互图 \(G_t^{type}\)，由“图构造函数” \(\Gamma^{type}(X_t, w_t)\) 根据联合状态生成。
- **MIP 编码关键点**：
  - 原子谓词用 Big-M 方法编码。
  - 逻辑与时间算子（否定、合取、析取、Until、Eventually、Globally）采用标准线性化约束。
  - 图算子编码分为四步：
    1. 确定满足边存在与权重区间 \(W\) 的入边（或出边）资格变量 \(\gamma^{type}_{j,i,t}\)；
    2. 用逻辑与编码“资格 ∧ 邻居满足 φ”的变量 \(y^{type}_{j,i,t}\)；
    3. 用整数基数约束实现计数区间 \([e_1,e_2]\)；
    4. 用析取/合取实现图类型上的存在/全称量化。
  - 多智能体量词 \(EX\)（存在）、\(FA\)（全称）分别用析取/合取约束编码。
- **SMT 编码关键点**：
  - 原子谓词用等价式 \(z_{i,t}^{\mu} = (a^\top x_t^i - b \ge 0)\) 表达。
  - 逻辑和时间算子用布尔连通与有限时间展开（unrolling）。
  - 图算子通过布尔变量 \(b^{type}_{j,i,t}\) 表示边存在及权值在区间内；用整数变量 \(c^{type}_{i,t}\) 表示计数；用 iff 约束计数区间；图类型量化用逻辑或/与。
- **正确性保证**：分别给出 Lemma 6/8（agent-local 不变式）与 Theorem 7/9（multi-agent 整体正确性），通过结构归纳证明满足 MIP/SMT 编码的赋值对应的轨迹一定满足 STL-GO 规范（可靠性保证）。
- **统一接口**：提供统一框架，可同时指定智能体约束、图拓扑、STL-GO 规范与目标函数，编译为 MIP 或 SMT 模型，便于直接对比。

## 3. 实验设计：数据集 / 场景 / benchmark / 对比方法

- **主实验场景**：多无人机搜索与救援（search-and-rescue）任务，基于 SwarmLab 仿真框架并扩展。
  - 环境：卫星地形图；无人机分为“定位器”（locators）与“救援器”（rescuers），角色异构（控制集不同，救援器最大速度低于定位器）。
  - 任务：在多个已知装配区（assembly areas）可能出现紧急情况，定位器需感知、通信、指派救援器，救援器需到达紧急地点并送至救援中心。
  - 使用“场景树”方式处理不确定性：为所有可能的紧急激活组合（\(\Omega \subseteq \{0,1\}^{|M|}\)）联合求解，且对不可区分的场景施加控制一致性约束。
- **规范内容**：五个 STL-GO 子规范（检测、中继、指派、到达、交付），包含感知图、通信图、任务图三种交互图。
- **对比方法**：
  - 主要对比 **MIP (Gurobi)** 与 **SMT (Z3)** 两种后端。
  - 针对 MIP 额外对比无目标函数、线性目标、二次目标三种目标设置。
  - 与 **HyperLTL**（基于 HypRL 基准）做编码规模对比。
- **消融实验**：
  - 按图复杂度递增：STL only（无图算子）、+Sensing（感知图）、+Communication（通信图）、+Task（任务图）。
  - 团队规模：5/7/9 个定位器、2/3/3 个救援器（共三组）。
  - 基准中 \(\Omega\)：\(|L|=5\) 时 \(|\Omega|=4\)，其他为 8。
- **第二基准**：从 HypRL 改编的 \(N\times N\) 网格世界（firefighter + medic），对比 STL-GO 与 HyperLTL 的编码规模和求解时间，取 \(N=5,7,10\)。

## 4. 资源与算力

- 论文中未明确提及 GPU 型号、数量或训练时长。实验均基于 CPU 求解器：
  - MIP 使用 Gurobi，SMT 使用 Z3。
  - 运行环境：计算集群，16 CPU 核心，64 GB 内存。
- 未涉及神经网络训练或 GPU 加速，因此无相关算力信息。论文也提到学习式方法不在本文范围内。

## 5. 实验数量与充分性

- **实验数量**：
  - 主基准包含 3 种图复杂度 × 3 种团队规模 = 9 组配置；
  - 每组配置同时运行 MIP（无目标、线性、二次）与 SMT（仅满足性），共约 36 组求解实例；
  - 另有 HyperLTL 对比实验：3 种网格大小 × 4 种方法组合（HyperLTL+SMT、STL-GO+SMT、HyperLTL+MIP、STL-GO+MIP）= 12 组。
- **充分性与公平性**：
  - 消融覆盖从无图算子到三种图算子叠加，能够清晰展示图复杂度对求解规模和耗时的影响。
  - 团队规模变化考虑了定位器与救援器数量，而非只改变总数，更为细致。
  - MIP 与 SMT 使用统一的规范和编码接口，对比相对公平。
  - 报告了最大单场景模型规模、总求解时间，信息充分。
  - 不过，实验主要集中在搜索-救援与网格两个场景，未包括随机生成的复杂环境或非凸障碍地图，通用性验证有限。

## 6. 论文的主要结论与发现

1. **STL-GO 规划可求解**：首次给出了 MIP 与 SMT 两种可靠编码，能够为包含多时变图约束的 STL-GO 规范生成可行轨迹。
2. **SMT 更轻量快速**：在几乎所有配置下，SMT 编码的变量数、约束数和求解时间都明显少于 MIP。例如，最难配置（9 locators, 3 rescuers, 全图）中 SMT 用时 16.5 秒，而 MIP 无目标需 1480 秒。
3. **MIP 适合目标优化**：MIP 能带目标函数（线性/二次），可生成更高质量轨迹；SMT 仅满足性，无优化能力。在无目标时 MIP 的解可能任意、不够直观。
4. **图算子显著增加复杂度**：从仅 STL 到添加感知、通信、任务图，约束数和求解时间逐步增长，尤其是任务图导致最大增幅。
5. **SMT + STL-GO 比 HyperLTL 扩展性更好**：在网格基准中，HyperLTL 因需要展开成逐对单元格约束，约束数呈 \(O(T\cdot N^4)\) 增长；STL-GO 通过图算子和连续值谓词编码，约束数随 \(T\) 线性增长。例如 \(N=10\) 时 HyperLTL+SMT 有 35.5 万约束，而 STL-GO+SMT 仅 2400 约束。
6. **为未来扩展奠定基础**：该集中式、全观测开环编码可作为分散式、部分可观测扩展的前提。

## 7. 优点

- **形式化贡献明确**：给出了 MIP 与 SMT 编码的可靠性证明（Lemma 及 Theorem），结构严谨。
- **图算子编码系统完整**：对边资格、权重区间、邻域基数、图类型量化、多智能体量化均给出了具体的编码步骤，填补了 STL-GO 规划空白。
- **统一接口设计好**：两种求解后端共享同一规范前端，支持目标函数，便于工程使用与横向比较。
- **实验设计全面**：同时考虑团队规模、图复杂度、目标函数三组变量，并额外提供 HyperLTL 对比，突出 STL-GO 的表达优势。
- **实际应用驱动**：搜索-救援场景贴近真实 UAV 任务，具有较强应用意义。

## 8. 不足与局限

- **假设较强**：
  - 环境与智能体动力学必须是确定性、已知的，且限制为仿射动力学；对非线性动力学需要近似或扩展至非线性 SMT 理论，未包含在本文中。
  - 图构造函数必须是 MIP/SMT-encodable（分段仿射/布尔表达式），限制了可表达的图类型（如复杂非线性相似度函数）。
- **仅开环集中式**：合成的是开环控制序列，只能在预计算场景树中分支，不能在线实时决策；论文也未处理随机性或对抗性环境。
- **可扩展性瓶颈**：MIP 在最难配置下会超时（报告为 best incumbent），SMT 虽快但也随问题规模增加；多图交互的组合复杂度仍然较高。
- **实验覆盖有限**：只用了两个仿真场景（SwarmLab 搜索救援、HypRL 网格），未在真实机器人平台或更复杂 3D 环境中验证；未报告求解内存峰值等资源指标。
- **与 HyperLTL 对比侧重编码规模**：HyperLTL 的基准为离散网格，未完全公平体现连续动力学下两者语义表达的差异；STL-GO 的图算子在纯符号层面更紧凑是预期结果，实验只是验证了这一点。
- **未给出量化鲁棒性**：本文只处理布尔满足，没有提供 STL-GO 的鲁棒度（robustness）优化，无法权衡轨迹质量与规范满足程度（除简单目标函数外）。

（完）
