---
title: "Agentic Router: An Execution-Grounded Continual Learning Approach With Memory"
title_zh: 代理路由器：一种具有记忆的执行接地持续学习方法
authors: "Yuxuan Chen, Rongpeng Li, Zhifeng Zhao, Yuntao Liu, Xing Xu, Honggang Zhang"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09184v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于执行后果感知的双路径智能体，在命令行操作中生成并选择行动
tldr: 针对CLI网络操作中LLM代理生成命令可能失败或引入风险的问题，提出执行锚定的双路径后果感知代理。它生成多个完整动作并预测后果，通过效用和风险重排序选择最终动作；同时用SSH反馈进行会话级LoRA更新，提升可行动作覆盖和选择质量，实现互补增益。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09184v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1838, \"height\": 869, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09184v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1836, \"height\": 956, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09184v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 918, \"height\": 697, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09184v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 906, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09184v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 906, \"height\": 688, \"label\": \"Figure\"}]"
motivation: 现有方法仅关注命令生成或配置正确性，未利用执行经验联合改进候选覆盖和动作选择。
method: 提出双路径代理：提案路径提取可复用操作经验为可检索指导，选择路径用真实SSH反馈进行会话级LoRA更新。
result: 在SONiC多轮会话中，框架提升了可行动作覆盖率和top-1执行成功率，两条适应路径互补。
conclusion: 执行锚定的迭代学习可有效提升LLM代理在关键网络操作中的安全性和成功率。
---

## 摘要
大语言模型（LLM）代理为基于命令行的网络操作提供了一种有前景的接口，但一个看似合理的命令在执行后仍可能失败或引入操作风险。现有方法主要关注命令生成或最终配置的正确性，并未利用执行接地的经验来联合改进候选覆盖和动作选择。我们提出了一种用于基于CLI的SONiC操作执行接地双路径后果感知代理，它生成多个完整动作，预测其执行后果，并通过效用和风险感知的重新排序来选择最终动作。提议侧路径将可复用的操作经验抽象为可检索的指导，以提高可行动作覆盖，而不修改提议LLM；而选择侧路径通过使用真实SSH反馈进行会话级LoRA更新来调整后果预测器，以提高条件选择质量。在使用不同Qwen3提议模型的多轮SONiC操作会话上的实验表明，该框架提高了可行动作覆盖和top-1执行成功率，并且两条适应路径在交互过程中提供了互补的增益。

## Abstract
Large language model (LLM) agents provide a promising interface for command-line-based network operations, but a plausible command may still fail or introduce operational risk after execution. Existing approaches mainly focus on command generation or final configuration correctness, and do not use execution-grounded experience to jointly improve candidate coverage and action selection. We propose an execution-grounded dual-path consequence-aware agent for CLI-based SONiC operations, which generates multiple complete actions, predicts their execution consequences, and selects the final action through utility- and risk-aware reranking. The proposal-side path abstracts reusable operational lessons into retrievable guidance to improve feasible-action coverage without modifying the proposal LLM, while the selection-side path adapts the consequence predictor through session-level LoRA updates using real SSH feedback to improve conditional selection quality. Experiments over multi-turn SONiC operation sessions with different Qwen3 proposal models show that the framework improves feasible-action coverage and top-1 execution success, and that the two adaptation paths provide complementary gains over interaction.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- 现代网络（如 SONiC）的操作任务主要通过命令行界面（CLI）完成，LLM 代理有望将自然语言意图直接转化为可执行命令。
- 然而，CLI 操作并非简单的文本生成：一个命令即使语法正确、可执行，也可能在特定网络状态下产生错误后果或引入运行风险。例如，当只有一个 BGP 对等体需要恢复时，执行 `clear bgp *` 会重置所有对等体，导致路由抖动和瞬时可达性丢失。
- 现有 LLM 网络自动化研究大多聚焦于命令生成或最终配置正确性，没有利用“执行后反馈”来持续改进候选覆盖与动作选择。
- 交互式代理（如 ReAct、Reflexion）虽然引入了行动—观察循环，但并未针对有状态、环境依赖的网络系统提供基于执行接地的、支持多轮可靠持续改进的机制。
- 核心挑战是：一次顶层动作失败（top-1 failure）具有歧义——无法判断是“候选集中根本没有可行动作”（覆盖不足）还是“可行动作存在但未被选为首位”（选择错误）。因此需要分离“候选生成”与“后果感知选择”两条路径，并分别进行适应。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

#### 核心思想
- 提出**执行接地双路径后果感知代理**（execution-grounded dual-path consequence-aware agent），将 CLI 操作建模为多轮动作级决策过程。
- 分离两条适应路径：
  - **提案侧（proposal-side）**：通过可检索的操作记忆改进候选动作的覆盖率，不修改提案 LLM。
  - **选择侧（selection-side）**：通过会话级 LoRA 更新后果预测器，改进对可用候选的排序质量。

#### 关键技术细节
- **系统模型**：
  - 在每轮 t，智能体接收用户意图 \(q_t\)、环境观察 \(o_t\) 和交互历史 \(H_{t-1}\)，构造决策上下文 \(x_t = (q_t, o_t, H_{t-1})\)。
  - 提案生成器 \(G_{\text{prop}}\) 根据 \(x_t\) 和操作指导 \(M_t\) 生成 K 个完整候选动作 \(A_t = \{a_t^{(1)}, ..., a_t^{(K)}\}\)。
  - 后果预测器 \(f_{\theta_t}\) 对每个候选估计执行后果 \(\hat{z}_t^{(k)}\)，并用两个评分函数 \(h_r\)（任务进度）和 \(h_\rho\)（操作风险）计算得分。
  - 通过效用-风险加权重排序选择最终动作：\(a_t^* = \arg\max (\hat{r}_t^{(k)} - \lambda \hat{\rho}_t^{(k)})\)。
  - 执行后得到实际后果 \(z_t^*\) 和验证结果 \(y_t^*\)（0/1）。

- **问题形式化**：
  - 定义可行动作覆盖率 \(c_t\)：候选集中是否至少存在一个动作能完成任务。
  - 成功概率分解为 \( \Pr(y_t^*=1) = \Pr(c_t=1) \cdot \Pr(y_t^*=1 | c_t=1) \)。
  - 优化目标为最大化多轮平均 top-1 成功率。

- **双路径适应机制**：
  - **提案侧操作记忆**：
    - 用轻量级 LLM 记忆提取器将执行记录蒸馏为结构化的可复用操作教训（如类型、意图、适用条件、建议/避免命令）。
    - 当前上下文被编码为检索查询，从记忆库中检索 top-R 条指导，注入提案提示中，引导候选生成。
  - **选择侧后果适应**：
    - 冻结基础权重 \(W_0\)，插入 LoRA 低秩更新 \(W_t = W_0 + \Delta W_t = W_0 + U_t V_t\)。
    - 使用会话内收集的真实执行记录（状态—动作—后果样本）计算损失 \(L_{\text{sel}}\)，更新预测器参数，从而改善后续重排序决策。

- **算法流程（Algorithm 1）**：
  1. 构建上下文 \(x_t\)。
  2. 检索 \(M_t\) 并生成候选集 \(A_t\)。
  3. 预测所有候选的后果和评分。
  4. 通过式 (7) 排序，选择 top-1 动作。
  5. 执行并评估；若 top-1 失败，则按排序依次执行其他候选，收集额外反馈用于适应。
  6. 更新选择侧 LoRA 参数。
  7. 周期性从更新缓冲中提取操作记忆，充实提案侧记忆库。

### 3. 实验设计：数据集 / 场景、基准（Benchmark）、对比方法

- **场景**：基于 SONiC 的 CLI 网络操作，包含多轮操作会话，覆盖查询（query）、配置（configuration）和诊断（diagnosis）三类任务。
- **数据集/任务序列**：固定序列共 **140 个交互回合**，所有方法使用相同的请求序列、初始环境状态、候选预算、SSH 执行接口和验证规则。
- **候选预算**：每轮生成 **K=5** 个候选动作，评估执行后的 top-1 成功率。
- **模型配置**：
  - 提案生成器：Qwen3-1.7B、Qwen3-8B、Qwen3-14B。
  - 后果预测器：Qwen3-0.6B，并附加奖励头与风险头。
  - 后果感知评分模块初始化自 300 条带有手工标注效用和风险标签的执行记录。
- **评价指标**：
  - 主要指标：top-1 执行成功率（由验证器判定）。
  - 辅助诊断指标：Recall@5（候选集中是否至少有一个可行动作，对应覆盖率 \(c_t\)）。
- **对比基线**：
  - **单提议基线**：直接执行第一个生成的候选。
  - **自一致性（Self-consistency）**：采样 5 个候选，通过多数投票选择，不使用执行反馈。
  - **无适应基线**：禁用提案侧检索和选择侧预测器适应。
  - **消融变体**：仅提案侧适应、仅选择侧适应、完整双路径框架。

### 4. 资源与算力

- 论文中**未明确说明**使用的 GPU 型号、数量、训练时长等算力信息。
- 仅能推断实验中使用了 Qwen3 系列的多个模型（1.7B、8B、14B 作为提案模型，0.6B 作为后果预测器），但未报告具体硬件环境和资源消耗。
- 因此，无法从论文中获取关于算力的具体量化信息。

### 5. 实验数量与充分性

- **实验数量**：
  - 不同提案模型规模（3 种）上的整体性能对比。
  - 在 140 轮交互上的端到端性能曲线（无适应 vs 完整双路径）。
  - 针对两条适应路径的消融实验（仅提案侧、仅选择侧、完整框架）。
  - 对比多种基线（单提议、自一致性、无适应）。
- **充分性评估**：
  - 实验覆盖了不同模型尺寸、不同适应机制，能够较好验证核心假设（候选覆盖与选择质量分离）。
  - 消融实验清晰展示了双路径的互补增益，支持论文的理论分解。
  - 但实验仅在单一场景（SONiC CLI）和固定任务序列上进行，未提供多种网络系统或任务分布变化下的结果；未报告方差或多次重复运行的结果，可靠性证据有限。
  - 总体而言，实验设计对验证方法内部逻辑是充分的，但泛化性和统计稳健性证据不足。

### 6. 论文的主要结论与发现

- 多候选提案能够提高可行动作的覆盖率，但单单覆盖不足以保证 top-1 成功；后果感知选择与执行接地适应对将可行动作转化为成功执行至关重要。
- 完整双路径框架在所有提案模型规模（1.7B、8B、14B）上都取得了最高的 top-1 成功率，且相比无适应基线有持续改进。
- 消融实验表明：
  - 仅提案侧适应通过改进可行覆盖带来提升；
  - 仅选择侧适应通过改进条件选择质量带来提升，但性能受限于候选集质量；
  - 两者互补，联合使用效果最佳，验证了式 (11) 的分解逻辑。
- 执行接地的持续学习能够有效提升 LLM 代理在关键网络操作中的安全性和成功率。

### 7. 优点

- **问题表述清晰**：将 top-1 失败分解为“覆盖失败”和“排序失败”，建立了基于执行反馈的双路径适应目标，具有理论指导意义。
- **双路径设计合理**：提案侧采用检索式操作记忆，避免修改生成模型，计算开销小且可复用；选择侧采用 LoRA 更新，保持基础预测器稳定，兼顾会话级塑性。
- **执行接地性强**：利用真实 SSH 反馈（尤其是失败后的额外探索）作为适应信号，而非依赖语言合理性，更贴近实际网络运维。
- **算法完整**：给出了清晰的伪代码（Algorithm 1），便于复现。
- **实验覆盖模型规模差异**：验证了不同容量提案模型下的有效性和互补性，增强结论可靠性。

### 8. 不足与局限

- **算力信息缺失**：未报告训练/推理的硬件资源、时间成本，不利于实际部署评估。
- **实验场景单一**：仅在 SONiC CLI 操作中进行评估，未验证在其他网络系统（如 Cisco IOS、FRR 之外的平台）的泛化能力。
- **任务序列固定**：140 个回合固定，可能无法反映真实运维中的动态意图分布和长期漂移。
- **统计稳健性不足**：未报告多次运行的平均值和方差，无法判断性能提升是否显著。
- **安全评估有限**：论文虽强调操作风险，但未系统评估危险命令的避免率或失败模式分析，未来工作才提及更全面的安全评估。
- **依赖 LLM 记忆提取器**：提案侧的课程抽取使用了轻量级 LLM，其自身质量可能成为瓶颈，论文未分析该组件失败影响。
- **额外执行开销**：在 top-1 失败后执行较低排名候选，会增加环境交互成本，论文未量化此开销的收益权衡。

（完）
