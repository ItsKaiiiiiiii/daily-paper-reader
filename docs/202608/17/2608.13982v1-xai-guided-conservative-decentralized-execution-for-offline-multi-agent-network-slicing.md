---
title: XAI-Guided Conservative Decentralized Execution for Offline Multi-Agent Network Slicing
title_zh: 基于XAI引导的保守分布式执行用于离线多智能体网络切片
authors: "Eslam Eldeeb, Hatim Chergui, Merouane Debbah"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.13982v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向网络切片的离线多智能体强化学习
tldr: "面向6G网络切片的资源分配存在资源耦合与竞争，在线MARL依赖环境交互和通信，成本高且不安全。提出X-CODE，一种可解释离线MARL，通过可解释性奖励塑形在集中训练中调整联合离线转移偏好，实现无通信的分散执行。实验显示零资源冲突，推理延迟降低88%。该框架兼顾可解释性与实用性，为离线多智能体学习提供新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13982v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1823, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1811, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1197, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 585, \"height\": 1057, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 584, \"height\": 1057, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 572, \"height\": 1057, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13982v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 575, \"height\": 1058, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13982v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 919, \"height\": 1030, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13982v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 446, \"label\": \"Table\"}]"
motivation: 现有在线MARL网络切片方法依赖环境交互与智能体通信，部署成本高、安全性差，难以实用。
method: 提出X-CODE，利用可解释性奖励塑形调整离线联合转移偏好，集中训练后实现无通信分散执行。
result: "测试中零资源冲突且降低切片延迟，与在线基线相比信令开销更低，有效推理延迟降低88%。"
conclusion: X-CODE验证了可解释离线MARL在资源切片中的潜力，兼顾性能与可部署性，提供开源实现。
---

## 摘要
面向第六代（6G）及超6G网络的最新进展加速了对智能资源管理机制的需求，这些机制需能在网络切片的共享基础设施下支持异构服务。然而，网络切片中的资源分配自然形成一个资源耦合的协作优化问题，其中切片需求相互竞争。切片竞争有限资源以最小化各自延迟，同时协调以避免冲突和利用不足。尽管多智能体强化学习（MARL）在此类场景中展现出有前景的性能，现有的在线公式因依赖环境交互和智能体间通信而仍然成本高昂、不安全且难以部署。在本工作中，我们提出了可解释人工智能（XAI）引导的保守分布式执行（X-CODE）。X-CODE是一种可解释的离线MARL，无需环境交互或智能体间通信即可离线运行。它利用可解释性感知的奖励塑形来修改集中训练期间联合离线转换的相对偏好，以改善分布式资源分配行为。部署时，智能体独立运行，无需智能体间的信令交换。仿真结果表明，所提方法在评估的测试回合中实现了零观察到的资源冲突事件，同时最小化每切片延迟。此外，与在线基线相比，所提框架在考虑的通信延迟模型下表现出更低的信令开销，并将有效推理延迟降低了88%。源代码和数据集可通过以下链接获取：https://github.com/Eslam211/xcode-ran-slicing。

## Abstract
The recent advances toward sixth-generation (6G) and beyond-6G networks have accelerated the need for intelligent resource management mechanisms capable of supporting heterogeneous services under shared infrastructures in network slicing. However, resource allocation in network slicing naturally forms a resource-coupled cooperative optimization problem with competing slice demands. Slices compete for limited resources to minimize individual latencies while coordinating to avoid conflicts and underutilization. Although multi-agent reinforcement learning (MARL) has shown promising performance in such settings, existing online formulations remain costly, unsafe, and difficult to deploy due to their reliance on environmental interactions and communication among agents. In this work, we present explainable artificial intelligence (XAI)-guided conservative decentralized execution (X-CODE). X-CODE is an explainable offline MARL that operates offline without environmental interaction, nor inter-agent communication. It exploits explainability-aware reward shaping to modify the relative preference among joint offline transitions during centralized training to improve decentralized resource-allocation behavior. In deployment, the agents operate independently without signaling exchange among the agents. Simulation results demonstrate that the proposed approach achieves zero observed resource-conflict events in the evaluated test episodes while minimizing per-slice latencies. Moreover, the proposed framework exhibits lower signaling overhead and reduces effective inference latency by 88 % under the considered communication-delay model compared to the online baselines. Source codes and datasets are available through: https://github.com/Eslam211/xcode-ran-slicing.

---

## 论文详细总结（自动生成）

# 论文总结：XAI-Guided Conservative Decentralized Execution for Offline Multi-Agent Network Slicing

## 1. 论文核心问题与整体含义

**研究背景与动机：**

- 6G及超6G网络要求智能资源管理机制，网络切片是实现多服务共享物理基础设施的关键技术。
- 网络切片中的资源分配本质上是"资源耦合的协作优化问题"：各切片（如eMBB、mMTC、URLLC）竞争有限的计算资源以最小化自身延迟，同时需要协调以避免资源冲突或利用不足。
- 传统集中式优化因维度爆炸而计算低效；在线MARL方法虽然性能优异，但存在以下严重缺陷：
  - 依赖持续的环境交互，训练成本高、周期长；
  - 早期探索可能产生不安全决策（如资源过度承诺）；
  - 智能体间通信产生高信令开销和延迟，通信受限时系统易失效。
- 离线MARL虽然避免了在线交互，但仍面临数据集覆盖度有限、OOD（分布外）动作导致过乐观估计、缺乏协调信号等挑战。

**核心问题：** 如何在无环境交互、无智能体间通信的约束下，利用离线数据训练出既能最小化切片延迟又能避免资源冲突的分布式策略。

## 2. 方法论：X-CODE框架

**核心思想：**

X-CODE是一种可解释的离线MARL框架，将保守Q学习（CQL）与值分解网络（VDN）结合，采用集中训练-分散执行（CTDE）范式，并利用XAI（SHAP值）进行奖励塑形，以引导离线学习朝向具有集中特征归因的转换方向优化。

**关键技术细节：**

- **POMDP建模：**
  - 动作：每个智能体从离散集合A={1,5,10,15,20,25,30}（单位：10⁹ CPU周期/s）中选择CPU分配；
  - 观测：每个智能体观测局部状态 $o_{k,t} = [\Phi_{k,t}, \beta_{k,t}, q^c_{k,t}, q^r_{k,t}]$（归一化负载、分配缺口、计算队列、传输队列）；
  - 奖励：$r_{k,t} = -\lambda_l L_{k,t+1} + \lambda_a \tilde{a}_{k,t} - \lambda_c \chi_t$；
  - 团队奖励定义为各智能体奖励的均值。

- **CQL-VDN损失函数：**
  - 集中值函数分解为各智能体Q函数之和：$Q(o,a) = \sum_k \tilde{Q}_k(o_k, a_k)$；
  - 损失包含VDN的Bellman误差项和CQL保守正则项（惩罚OOD动作的高Q值）。

- **XAI引导的奖励重标注（核心创新）：**
  - 将数据集分为M折，每折用其余数据训练初步CQL-VDN批评者，避免自解释偏差；
  - 对保留的转换计算SHAP值，将绝对SHAP值映射为特征归因分布；
  - 计算归因熵 $H_k$，低熵表示归因集中在少数主导特征上；
  - 定义归因集中度分数 $b_{VDN}(o,a) = \frac{1}{K}\sum_k(1 - \frac{H_k}{\log d_o})$；
  - 重标注奖励：$r_{XAI} = r_{team} + \mu(b_{VDN}(o,a) - \bar{b}_{VDN})$，其中中心化项保持奖励尺度，μ控制塑形强度。

- **训练流程（Algorithm 1）：**
  - 划分数据折 → 训练预批评者 → 计算SHAP和集中度分数 → 重标注奖励 → 用重标注后的数据集重新训练CQL-VDN → 提取各智能体分散策略。

**部署方式：** 各智能体仅依赖本地Q函数 $\pi_k(o_k) = \arg\max_{a_k} \tilde{Q}_k(o_k, a_k)$ 独立决策，无需任何智能体间通信。

## 3. 实验设计

**场景参数：**

- 3个网络切片（智能体）：eMBB、mMTC、URLLC；
- 共享边缘服务器CPU预算：40 Gcycles/s；
- 默认可行分配：$f^{th}=[15,15,10]$；
- 决策间隔 τ = 0.01s，每条轨迹40步；
- 离线数据集：10,000条转换，由**随机游走行为策略**收集（低质量、无协调信息的数据集）；
- 奖励系数：$\lambda_c=30$，$\lambda_l=150$，$\lambda_a=0.05$。

**对比基线：**

- 在线方法：DQN (No Comm / No IB)（无通信独立DQN）、DQN (Comm + IB)（带通信编码和信息瓶颈的在线DQN，参考文献[37]）；
- 离线方法：X-CQL（XAI引导的独立CQL）、CFCQL（反事实保守Q学习）、QMIX（单调解分解）、VDN（无奖励重标注的值分解网络）；
- 启发式基准：随机游走策略。

**评估指标：** 每步奖励、平均延迟、冲突率、延迟CDF、冲突CDF、CPU利用率、信令开销、推理时间。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量、训练时长或具体硬件资源，仅提供了以下训练配置信息：
  - 在线训练：500个episode，批大小64，学习率10⁻³，使用Adam优化器，目标网络软更新系数0.005，回放缓冲区10,000；
  - 离线训练：1,500梯度步，批大小256，学习率10⁻⁴，CQL参数α=1，塑形强度μ=1；
  - 神经网络结构：3个隐藏层，每层128个神经元。
- Q函数建模使用全连接神经网络，但未提及具体硬件平台。

## 5. 实验数量与充分性

**实验分组：**

1. **在线基线评估（图2）**：对比DQN (Comm + IB)与DQN (No Comm / No IB)的每步奖励、平均延迟、冲突率随训练进程的变化；
2. **数据集覆盖分析（图3）**：对比在线专家数据集与随机游走数据集的行动覆盖分布；
3. **离线性能评估（图4）**：延迟CDF和冲突CDF对比，包含所有基线和随机策略；
4. **离线MARL基线对比（图5）**：与X-CQL、CFCQL、QMIX、VDN的延迟CDF和CPU利用率对比；
5. **超参数消融（图6）**：分别扫描CQL参数α（0, 1, 10）和塑形强度μ（0, 0.5, 1, 5）；
6. **XAI洞察分析（图7）**：SHAP值的KDE分布和各特征的贡献度分析；
7. **复杂度分析（图8）**：信令开销随智能体数量的变化、不同通信延迟下的推理时间对比。

**充分性与公平性评估：**

- 实验覆盖面较广，包含在线/离线对比、多个MARL基线、消融研究和复杂度分析；
- 离线对比中统一了网络架构、CQL架构和超参数，保证了基线对比的公平性（如CFCQL和QMIX都构建在相同CQL架构之上）；
- 超参数消融设计合理，分别验证了保守正则项和XAI塑形的独立贡献；
- 但所有实验仅在单一仿真场景（3个切片、固定流量模式）下进行，缺乏多场景、多流量配置下的泛化验证，随机游走数据集虽具有挑战性但属于低质量数据，未对多质量级别的数据集进行系统评估。

## 6. 主要结论与发现

1. **零冲突达成**：X-CODE是唯一在评估测试回合中实现零资源冲突事件的方案（图4b），优于所有在线基线和离线基线；
2. **延迟性能接近在线最优**：X-CODE的延迟中位数与在线DQN (Comm + IB)几乎对齐，显著优于无通信在线基线和随机策略（图4a）；
3. **资源利用更高效**：X-CODE在离线训练中逐步提高CPU利用率至高位区域，而CFCQL、QMIX和X-CQL均收敛到保守的低利用策略（图5b）；
4. **可解释性洞察**：SHAP分析揭示CPU缺口（β）和流量（Φ）特征主导集中值估计，不同智能体具有不同的特征主导模式（图7），说明学习到的分配策略具有切片依赖性；
5. **大幅降低通信开销和延迟**：相比在线DQN (Comm + IB)，信令开销降低100%（零信令），有效推理延迟平均降低88%（图8）；
6. **超参数影响**：α=1和μ=1分别为最优的保守正则和塑形强度；α=0导致过乐观估计，α=10导致过度保守；μ=0退化为保守的VDN基线，μ过大则XAI奖励主导原环境奖励。

## 7. 优点

- **方法论创新性**：首次将XAI（SHAP）奖励塑形引入离线MARL网络切片场景，通过归因集中度分数引导离线学习，而非仅作为事后解释工具；
- **实用性强**：无需环境交互和智能体间通信即可完成训练和部署，显著降低信令开销和推理延迟，适合实际网络部署；
- **安全性与可靠性**：CQL保守正则有效抑制OOD动作的过乐观估计，离线训练本身避免了在线探索的安全风险；
- **可解释性**：通过SHAP分析揭示了各特征（流量、CPU缺口、队列长度）对集中值估计的贡献，为运营商提供了决策透明性；
- **消融研究完整**：分别验证了CQL和XAI塑形各自的贡献，超参数扫描全面；
- **开源可复现**：提供了源代码和数据集。

## 8. 不足与局限

- **实验规模有限**：仅评估了3个智能体的单一边缘服务器场景，未扩展到更大规模（更多切片数、多服务器、多基站）的真实网络环境；
- **未说明硬件资源**：论文未报告GPU型号、数量、实际训练时长等计算资源信息，无法评估方法的实际计算门槛；
- **数据集单一性**：仅使用了随机游走数据集进行离线训练，未系统评估不同质量级别（如不同专业程度、不同覆盖度的数据集）下的性能表现；
- **流量模式固定**：实验采用单独的固定流量配置，缺乏对不同流量分布、突发流量、非平稳流量条件下的鲁棒性验证；
- **策略不变性限制**：作者明确指出所提奖励重标注不是基于势函数的塑形，不保证最优策略不变性，μ的值需要谨慎调节，存在引入优化偏差的风险；
- **与现实部署存在差距**：实际RAN网络存在更多复杂约束（如频谱资源、前传后传带宽、能量消耗、用户移动性等），该框架在这些维度的适用性未验证；
- **对未来方向的讨论不足**：离线到在线的微调（offline-to-online fine-tuning）虽被提及为未来工作，但缺少初步探索或可行性分析。

**（完）**
