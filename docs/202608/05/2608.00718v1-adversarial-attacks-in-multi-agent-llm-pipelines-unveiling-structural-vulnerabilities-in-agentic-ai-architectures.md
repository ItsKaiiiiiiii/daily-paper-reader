---
title: "Adversarial Attacks in Multi-Agent LLM Pipelines: Unveiling Structural Vulnerabilities in Agentic AI Architectures"
title_zh: 多智能体LLM流水线中的对抗性攻击：揭示智能体AI架构中的结构性漏洞
authors: "Faisal Haque Bappy, Tahrim Hossain, Tarannum Shaila Zaman, Raiful Hasan, Kamrul Hasan, Tariqul Islam"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00718v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体架构的结构性脆弱性
tldr: 多智能体LLM管道将多个模型串联，对抗内容一旦被某智能体接受，便会作为可信输入在后续流程中传播，根因是缺少跨边界验证。利用GAIA和SWE-Bench生产轨迹，归纳出内容注入、代理冒充、计划偏离和记忆中毒四类结构性攻击面。在GPT-5-mini、Claude Sonnet 4.5与Kimi K2.5同配置下实验，攻击成功率与管道结构强相关，与模型能力无关。这证明对抗脆弱性是架构固有属性，防御必须提升到管道级。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.00718v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 945, \"height\": 200, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00718v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 947, \"height\": 211, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00718v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 946, \"height\": 214, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00718v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 941, \"height\": 242, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00718v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 666, \"height\": 434, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00718v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1905, \"height\": 414, \"label\": \"Figure\"}]"
motivation: 多智能体管道因缺少边界验证，使对抗内容可沿流程传播，产生内容注入、代理冒充等独特攻击面。
method: 用GAIA和SWE-Bench生产轨迹分析漏洞，并在受控多智能体设置中对三种模型进行对抗评估。
result: 攻击成功率由管道结构而非模型能力决定，GPT-5-mini、Claude Sonnet 4.5和Kimi K2.5在相同配置下均呈结构性脆弱。
conclusion: 这揭示对抗脆弱性是架构固有属性，防御须转向管道级边界验证，为多智能体安全设计提供新方向。
---

## 摘要
多智能体LLM流水线将多个专门的语言模型智能体编排成结构化工作流，中间输出在各智能体间传递以解决复杂任务。这种设计引入了单智能体设置中不存在的安全漏洞：一旦某个智能体接受了对抗性内容，它就会作为可信输入在整个流水线中传播。我们认为，这一漏洞源于边界验证的缺失——这是一种安全原语，要求在数据跨越智能体间边界时执行显式验证，包括内容、身份、执行意图和状态完整性。缺乏此类验证使得现代流水线嵌入了隐式的信任假设，这些假设不具备对抗鲁棒性，从而产生了结构上不同的攻击面（例如内容注入、智能体冒充、计划偏离和记忆中毒）。利用GAIA和SWE-Bench基准测试中标注的生产轨迹，我们表明这些漏洞在良性部署中也会出现，并且在很大程度上规避了现有的评估框架。我们进一步在受控的多智能体环境中操作化这些失败模式，并在GPT-5-mini、Claude Sonnet 4.5和Kimi K2.5上以完全相同的流水线配置进行评估。结果表明，攻击成功与流水线结构而非模型能力相关，这表明对抗性漏洞本质上是一种架构属性，并推动向流水线级防御转变。

## Abstract
Multi-agent LLM pipelines orchestrate multiple specialized language model agents into structured workflows where intermediate outputs are passed across agents to solve complex tasks. This design introduces a security gap absent in single-agent settings: once an agent accepts adversarial content, it is propagated as trusted input throughout the pipeline. We argue that this vulnerability stems from the absence of boundary verification, a security primitive that enforces explicit validation of data as it crosses inter-agent boundaries, including content, identity, execution intent, and state integrity. Without such verification, modern pipelines embed implicit trust assumptions that are not adversarially robust, giving rise to structurally distinct attack surfaces (e.g., content injection, agent impersonation, plan deviation, and memory poisoning). Leveraging annotated production traces from the GAIA and SWE-Bench benchmark, we show that these vulnerabilities arise in benign deployments and largely evade existing evaluation frameworks. We further operationalize these failure modes within a controlled multi-agent setting and evaluate them across GPT-5-mini, Claude Sonnet 4.5, and Kimi K2.5 under identical pipeline configurations. The results reveal that attack success aligns with pipeline structure rather than model capability, indicating that adversarial vulnerability is fundamentally an architectural property and motivating a shift toward pipeline-level defenses.

---

## 论文详细总结（自动生成）

# 多智能体LLM流水线中的对抗性攻击：揭示智能体AI架构中的结构性漏洞

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **研究背景**：多智能体LLM系统已成为复杂任务执行的广泛范式。通过AutoGen、LangChain等框架，多个专门化智能体以结构化工作流协作，在GAIA、SWE-bench等基准上表现优异。然而，这种设计引入了一个单智能体环境中不存在的安全缺口：当智能体之间交换中间输出且缺乏显式验证时，某一阶段接受的对抗性内容会被当作可信输入传播给下游智能体，产生**跨边界传播效应**。
- **核心问题**：论文提出，多智能体管道存在三类系统性未经验证的边界——**内容边界**（外部检索数据与可执行指令之间）、**委托边界**（声明的执行计划与实际执行之间）、**身份边界**（合法智能体与占用同一路由槽位的任意进程之间）。边界验证的缺失使得管道运行在"隐式信任假设"之上，每个边界都成为对抗性利用的独立入口点。
- **整体含义**：作者的核心论断是——多智能体LLM管道的对抗脆弱性在本质上是**架构属性**而非**模型能力**的函数。这意味着仅提升单模型的安全性或指令遵循能力无法解决根本问题，防御投资应转向**管道级边界验证**等结构性机制。

## 2. 论文提出的方法论

### 2.1 核心思想

论文提出需要将"边界验证"（Boundary Verification）确立为多智能体LLM管道的必要安全原语——即在数据跨越智能体间边界时执行显式验证，涵盖内容、身份、执行意图和状态完整性。

### 2.2 形式化框架

将管道建模为有向无环图 **G = (A, E)**，其中 A 为智能体集合，E 定义消息传递依赖。管道输出为：
**y = (aₙ ◦ aₙ₋₁ ◦ ⋯ ◦ a₂ ◦ a₁)(τ)**（拓扑序执行）

三类边界约束的形式化：

| 边界类型 | 形式化约束 | 含义 |
|---------|-----------|------|
| 内容边界 | C_data ∩ C_instr = ∅ | 外部检索数据与操作者指令应类型分离 |
| 委托边界 | π̂ = π | 实际执行序列应与声明的子任务序列绑定 |
| 身份边界 | id(p) = aᵢ ⟺ verify_cred(p, aᵢ) = 1 | 进程身份需经凭证验证，而非仅凭路由位置 |

### 2.3 轨迹驱动的威胁建模方法

论文采用**经验驱动**而非合成假设的方法：

- 分析TRAIL基准中的147条标注生产轨迹（含836个人工标注错误实例），覆盖GAIA和SWE-Bench Lite两种任务设置
- 所有错误实例经三个筛选标准：**对抗可诱导性**（可由注入内容/信号触发）、**结构可复现性**（相同管道位置跨轨迹复现）、**跨边界传播性**（能污染起源之外的智能体跨度）
- 最终归纳出四类满足全部标准的漏洞类别，共覆盖452/836个错误（占54%），出现在108/147条轨迹中；而所有147条轨迹在TRAIL评分下均为"完美安全评分"，证明现有自动化评估无法发现这些漏洞

### 2.4 四类攻击的威胁模型

| 攻击类别 | 利用边界 | 攻击机制 | 轨迹证据 |
|---------|---------|---------|---------|
| **A1: 提示注入**（经检索内容） | 内容边界 | 攻击者控制检索路径中任一资源，嵌入指令性文本，使检索token同时属于C_data和C_instr | 102/147条轨迹(69.4%)；如轨迹5a6c51d5声称"经内部调查发现"却无对应工具调用跨度 |
| **A2: 共识投毒** | 委托边界 | 编排器接受任一子智能体结果且无法定人数校验；单条被投毒的高置信度响应即可决定最终输出 | 78/147条轨迹(53.1%)，97.3%评为高影响 |
| **A3: 计划劫持**/强制提前终止 | 委托边界 | 注入触发信号使执行轨迹在步骤 j < k 处截断，编排器直接调用final_answer | 56/147条轨迹(38.1%)；如轨迹4a8d094e步骤1失败后即得出"diamond"结论 |
| **A4: 静默智能体替换**/Sybil攻击 | 身份边界 | 重试期间，攻击者控制的Sybil进程在合法智能体的路由槽位响应，凭位置即可被视为合法 | 77/147条轨迹(52.4%)，SWE-Bench集中度(83.9%)高于GAIA(44.0%) |

### 2.5 理论论证

攻击成功概率被形式化为：
**S(G, M_LLM, Aₖ) = Pr[Bₖ(m, (aᵢ, aⱼ)) = 0]**

关键在于**右侧不包含M_LLM项**——攻击成功是图结构G的属性，更换基础模型不改变攻击成功率。每个Bₖ代表管道在有效层级缺失的检查：检索接口处的净化、编排器聚合边的计划到轨迹执行强制、路由层的凭证验证。这些检查无法由模型自行完成，因为它们作用于跨智能体边界传递的消息，而非单一上下文内的token。

## 3. 实验设计

### 3.1 使用的数据集与基准

- **TRAIL基准**（轨迹分析阶段）：147条标注生产轨迹，836个人工标注错误实例
  - **GAIA**任务设置：两层级管道（CodeAgent → ToolCallingAgent）
  - **SWE-Bench Lite**任务设置：单CodeAgent处理大型代码仓库
- **受控多智能体模拟**（对抗评估阶段）：20个共享任务，0.80基线准确率

### 3.2 实验架构

五层管道架构（10个智能体）：
1. **编排层**：Orchestrator 路由任务
2. **规划层**：Planner 分解子任务
3. **并行执行层**：2个Researcher（信息检索）、Analyst（推理）、Coder（代码生成/执行）
4. **审查层**：Reviewer（计划合规性）、Critic（一致性/置信度检查）
5. **输出层**：Summarizer（综合）、Validator（最终一致性检查）

### 3.3 对比方法与模型

三个基础模型（保持管道架构完全一致，仅改变骨干模型）：
- **GPT-5-mini**（OpenAI）
- **Claude Sonnet 4.5**（Anthropic）
- **Kimi K2.5**（Moonshot AI）

选择标准：跨越不同训练方法论和供应商，覆盖不同指令遵循能力，且未针对智能体间交互进行明确的安全调优，代表当前生产部署中的典型模型。

### 3.4 攻击变体与评估指标

- 每类攻击含3个逐渐升级的载荷变体
- 四个评估指标（对应四个研究问题RQ1-RQ4）：
  - **攻击成功率**（Attack Success Rate）：回答哪个边界最易被利用
  - **任务准确率**（Task Accuracy）：回答注入成功的端到端影响
  - **恢复率**（Recovery Rate）：回答管道能否自我纠正
  - **传播跳数**（Hop Count, HC）：回答攻击影响的传播深度

## 4. 资源与算力

- **论文中明确提到的算力信息**：作者提供了代码仓库链接（https://github.com/SPaDeS-Lab/adversarial-llm-pipeline），但**正文中未报告**所使用的GPU型号、数量、训练/推理时长、计算成本等任何具体的硬件资源信息。
- **间接推断**：实验规模为1,080次攻击运行 + 90次清洁基线运行，每次运行涉及10个智能体在五层管道上的完整执行——这一规模在学术实验中属于中等偏轻量，使用单卡或少量现代GPU即可完成，但具体资源配置无从得知。

## 5. 实验数量与充分性

### 5.1 实验量统计

| 实验类型 | 数量 |
|---------|------|
| 轨迹分析 | 147条生产轨迹，836个错误实例 |
| 对抗评估攻击运行 | 1,080次（20任务 × 4攻击 × 3载荷变体 × 3模型） |
| 清洁基线运行 | 90次（20任务 × 3模型 × 若干重复） |
| 攻击类别 | 4类（A1-A4） |
| 每个攻击的载荷变体 | 3级 |
| 测试模型 | 3个 |

### 5.2 充分性与客观性评估

**做得较好的方面**：
- 实验设计精准地隔离了核心变量——保持管道架构不变，仅更换骨干模型，从而可以直接将结果差异归因于模型或架构
- 轨迹分析与受控实验相结合，先证明漏洞存在于真实部署，再以量化方式验证其可利用性
- 四种指标从不同维度刻画攻击效果，形成互补证据链

**可能存在的局限性**：
- 仅使用20个共享任务，任务多样性有限，难以覆盖广泛的真实业务场景
- 仅测试3个模型；虽然覆盖了主流供应商，但更大的模型样本量能增强结论的普适性
- 未进行防御措施的消融实验（如加入边界验证机制后攻击成功率的变化），缺少对论文提出的防御方向的直接实证支持
- 没有与现有攻击框架（如AgentDojo、Agent Security Bench）在相同条件下的横向对比，虽然引用了相关工作进行定位，但缺少量化对比

## 6. 论文的主要结论与发现

### 6.1 边界可利用性层级（RQ1）

攻击成功率的**模型间分布一致**：
- **A1 提示注入**：0.61–0.72（最有效）→ 内容边界是主要入口点
- **A3 计划劫持**：0.81–0.86（紧随其后）→ 对Planner的集中信任是关键弱点
- **A2 共识投毒**：0.79–0.83 → 单点投毒即可控制输出
- **A4 Sybil替换**：0.76–0.78 → 需要多位置配合但仍有效

所有攻击类别成功率均超过0.6，**没有任何边界是可防御的**。

### 6.2 端到端影响（RQ2）

注入成功会转化为用户可见的任务失败（准确率显著低于0.80基线），而非仅停留在管道中间阶段。

### 6.3 恢复能力（RQ3）

恢复率普遍极低（最高0.22，A1和A3下所有模型低于0.10）——注入成功后的管道无法可靠恢复正确性。A1和A2载荷可传播4-6跳，贯穿完整执行到输出路径才遇到可能的干预点。

### 6.4 架构决定性（RQ4）

- 模型间成功率差异窄（A1最大仅0.07），而攻击类型间差异宽（任一模型上A1与A3差0.25），**四倍比率**表明管道结构是主导变量
- 缺失的检查（密码学证明、法定人数执行、计划验证机制）无论骨干模型如何都被一致利用
- 模型选择仅提供**边际性、二阶抵抗**，架构加固才是实质性改进

## 7. 论文的优点

1. **"轨迹驱动"方法论**：不同于现有工作（AgentDojo、ASB）采用合成攻击构造，论文从真实生产轨迹中归纳漏洞类别，确保威胁模型与实际部署中的失败模式对应，结论具有更高的生态效度。
2. **边界验证的形式化框架**：三类边界及对应公式（内容分区、委托绑定、身份凭证验证）清晰且可操作，为后续研究工作提供了统一的理论语言。
3. **关键的变量隔离实验设计**：保持管道架构恒定、仅变换骨干模型，从实验上分离"模型效应"与"结构效应"，是论文最核心的方法论贡献。
4. **发现现有评估框架的盲区**：所有147条轨迹在TRAIL评分均为满分，却存在大量结构性漏洞——这一发现揭示了当前安全评估工具的严重不足，有重要实践意义。
5. **理论论证与实践验证的一致性**：公式推导（S(G, M_LLM, Aₖ)中不含M_LLM项）与实验结果（模型间差异小、攻击类型间差异大）相互印证，论证链条紧凑。

## 8. 不足与局限

### 8.1 实验覆盖不足

- **任务数量有限**：仅20个共享任务，评估广度难以支撑强普适性结论
- **模型覆盖面**：3个模型虽来自不同供应商，但均为商业API模型，未包含开源模型（如Llama、Qwen等）对比
- **防御验证缺失**：论文呼吁"管道级防御"，但未实证测试任何防御机制的防护效果
- **攻击场景简洁性**：模拟管道包含10个智能体和5层，真实生产系统的复杂度和异构性可能远超此设置

### 8.2 偏差风险

- **TRAIL评分本身的置信度**：将所有147条轨迹评为"完美安全"可能反映的是TRAIL评估框架的能力边界而非真实安全状态，但这恰恰被论文用作论据而非受控变量
- **可选性偏差**：TRAIL轨迹可能不完全代表所有多智能体部署的类型分布（GAIA/SWE-Bench的领域局限性）
- **攻击载荷的生态效度**：3级变体虽考虑了递进性，但真实攻击者的自适应能力和创造力可能远超模拟范围

### 8.3 应用限制

- **无法证明全模型集合上的结论**：论文理论声称"攻击成功与M_LLM无关"，但实验仅覆盖3个模型，理论上无法排除其他模型（如更强安全对齐的模型）表现出不同模式
- **防御机制未落地**：论文推荐密码学证明、PBFT式法定人数提交和带外审计，但未讨论这些方案的性能开销、部署复杂度和对延迟的影响
- **缺少真实世界攻击验证**：所有受控攻击均在实验室模拟管道中执行，未在任何真实生产系统（如AutoGen部署）上验证

**总体评价**：这是一篇问题定义精准、方法论扎实、论证逻辑严密的安全研究。其核心贡献——将对抗脆弱性定性为架构属性而非模型属性——对多智能体系统的安全性设计具有重要的方向指示意义；但其影响范围受限于实验规模较小、防御验证缺失等实际问题。

（完）
