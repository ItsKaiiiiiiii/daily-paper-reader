---
title: "Retriever: Composing Closed-Loop Asynchronous Robot Programs"
title_zh: Retriever：组合闭环异步机器人程序
authors: "Linfeng Zhao, Haojie Huang, Jiayuan Mao, Weiyu Liu, Mykel Kochenderfer, Lawson L. S. Wong"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17213v1"
tags: ["query:agent"]
score: 9.0
evidence: 异步机器人智能体架构，包含规划与控制
tldr: 构建长时域机器人闭环系统时，组件时钟和延迟差异导致行为依赖调度，难以复现与调试。本文提出Retriever，涵盖异步决策模型、编程模型与运行时，将代理表示为有状态因果流函数图，编译为多后端运行时。该系统支持确定性重放与跨环境调试。真实机器人实验验证了有效性，显著提升了系统的可复现性与可重用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17213v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 880, \"height\": 927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1665, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 915, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1716, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 905, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1773, \"height\": 1043, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1812, \"height\": 464, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1663, \"height\": 602, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17213v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1812, \"height\": 464, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17213v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1557, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17213v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1123, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17213v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 855, \"height\": 394, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17213v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17213v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1484, \"height\": 249, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17213v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 916, \"height\": 230, \"label\": \"Table\"}]"
motivation: 现有机器人闭环系统因临时并发和发布/订阅约定导致行为依赖调度，难以复现与调试。
method: Retriever基于连续时间流异步环境-代理循环，将代理表示为状态因果流函数图，编译为多后端运行时。
result: 真实机器人案例研究验证了系统可行性，运行时开销可控且支持确定性重放。
conclusion: Retriever提供了覆盖全栈的异步机器人程序方案，提升长时域系统的可复现性和可调试性。
---

## 摘要
构建长期任务机器人代理需要组合闭环流水线——感知、信念更新、规划和控制——这些组件的运行时钟不同且延迟可变。如今，这些系统通常采用即兴的并发和发布/订阅约定来组装，这使得时序和输入消费语义变得隐式，从而产生依赖调度且难以复现、调试和复用的行为。当前的解决方案通常要么在算法层，要么在系统层解决部分问题，但不能同时解决。在这项工作中，我们提出了Retriever，它涵盖了整个堆栈：异步决策模型、编程模型、运行时以及一个示例闭环代理流水线。Retriever将代理表示为在显式运行时钟上执行的有状态因果流函数的图。我们通过连续时间流上的异步环境-代理循环形式化了这一观点，并展示了有限记忆因果策略可以由这些操作符的组合来表示。Retriever将这些图编译成支持多个后端的运行时，从而实现跨运行环境的系统调试以及从记录的异步数据进行确定性重放。我们通过真实机器人案例研究以及对运行时开销和确定性重放行为的受控研究来评估Retriever。

## Abstract
Building long-horizon robot agents requires composing closed-loop pipelines -- perception, belief update, planning, and control -- whose components run at different clocks and with variable latency. Today, these systems are often assembled with ad-hoc concurrency and pub/sub conventions that make timing and input-consumption semantics implicit, yielding schedule-dependent behavior that is hard to reproduce, debug, and reuse. Current solutions typically solve parts of this problem at either the algorithmic or the systems layer, but not both. In this work, we propose Retriever, which spans the entire stack: an asynchronous decision model, a programming model, a runtime, and an example closed-loop agent pipeline. Retriever represents an agent as a graph of stateful causal stream functions executed on explicit run clocks. We formalize this view via an asynchronous environment-agent loop over continuous-time streams and show that finite-memory causal policies can be represented by compositions of these operators. Retriever compiles these graphs into a runtime that supports multiple backends, enabling systematic debugging across running environments and deterministic replay from logged asynchronous data. We evaluate Retriever through a real-robot case study together with controlled studies of runtime overhead and deterministic replay behavior.

---

## 论文详细总结（自动生成）

# Retriever：组合闭环异步机器人程序 - 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：构建长期任务（long-horizon）机器人代理需要组合闭环流水线，包括感知、信念更新、规划、控制等组件。这些组件运行于不同时钟频率且具有可变延迟。目前系统采用临时并发和发布/订阅约定，使得时序和输入消费语义隐式，导致行为依赖调度器、难以复现、调试和重用。
- **背景**：现有解决方案要么在算法层（如MDP/POMDP），要么在系统层（如ROS、Dora），但缺乏同时覆盖两层的统一抽象。深度学习得益于模块化和组合性（如PyTorch层），但机器人领域缺乏显式时序组合的抽象。
- **整体目标**：提出一个覆盖全栈的异步机器人程序方案，包括异步决策模型、编程模型、运行时和示例闭环代理流水线，提升长时域系统的可复现性和可调试性。

## 2. 方法论：核心思想与关键技术细节

- **核心思想**：将代理表示为**有状态因果流函数图**，在显式运行时钟上执行。每个模块称为`Flow`，由`step()`函数、`clock`（时钟）和`input ports`（输入端口）组成。通过`sync`策略（如`Latest()`、`Window()`）在边缘确定性地同步异步输入，形成确定性的输入快照。
- **异步环境-代理循环**：扩展标准MDP，移除单一全局时间步。环境因果地将动作流映射到观测流，代理因果地将观测历史映射到动作流，事件在连续时间流上带时间戳。
- **关键抽象**：
  - `Flow`：状态同步步骤函数，绑定`Clock`（周期或触发）。
  - `Pipeline`：Flows通过`then()`连接成图，指定同步策略。
  - 同步策略：确定性地从上游历史中选择输入快照（如Latest、Window）。
  - 时间块（Temporal Chunking）：`PlanChunk`和`ActionChunk`充当时间扩展输出，使慢规划与快技能执行重叠。
- **公式**：每个Flow在局部时间步`t_k`执行`(h_{t_k+}, y_{t_k}) = f(h_{t_k-}, x_{t_k})`，其中`x_{t_k}`由同步策略从已提交上游历史中组装。反馈环为严格因果（延迟边），避免瞬时循环。
- **表示性定理**：任何具有局部有限决策时间、固定大小记忆和确定性同步策略的因果策略，均可由Retriever图实现。
- **功能性确定性定理**：给定确定性Flow代码、同步策略、初始状态、外部输入历史和时钟/提交追踪，输出历史唯一且独立于运行时调度（支持确定性重放）。
- **运行时**：Python DSL定义图，编译为静态中间表示（IR），可映射到单进程步进、多进程、Dora等后端。

## 3. 实验设计

- **场景与数据集**：
  - **真实机器人案例**：双机械臂PiperX平台，外接RealSense和腕部相机。两个任务：
    1. **香料搜索与调味**：四个抽屉中随机放置香料瓶，机器人需搜索、识别、取放、调味。
    2. **购物袋检索与分类**：变形袋中多个物品，需取出并按类别放入不同盘。
  - 低层技能策略训练数据：约200-300个完整专家演示（每个1-3分钟），合计几小时机器人数据。使用π0.5微调或多任务扩散策略从头训练。
  - **受控实验**：消息延迟基准（payload大小对比ROS 2 Python/C++、Dora）；确定性重放（混合物理弹球系统，对比发布/订阅vs事件时间）。
- **基准对比**：
  - 消融实验：去计划分块、去进度预测、去信念记忆、去重规划。
  - 端到端基线：π0.5（VLA only）、Diffusion policy only。
  - 系统对比：ROS 2、Dora、单进程Retriever等消息延迟。
  - 确定性对比：发布/订阅（模拟调度抖动）vs Retriever事件时间语义。
- **评价指标**：任务进度（percentage）、预计总时间（最佳进度归一化）、消息延迟、梯度分布一致性。

## 4. 资源与算力

- **未明确说明**：文中未提及所使用的GPU型号、数量或训练时长。仅提到低层技能训练使用“约200-300个完整专家演示，每个1-3分钟，合计几小时机器人数据”。涉及VLM（如Gemini）和VLA（π0.5）推理，但无训练硬件细节。
- **可能资源**：π0.5微调或从头训练扩散策略可能需要GPU加速，但具体未报告。

## 5. 实验数量与充分性

- **能力实验**：两个真实机器人任务，每个任务有多轮运行（Table IV给出进度和预计时间）。消融分析包含四个特征（计划分块、进度预测、信念记忆、重规划），分别测试其影响。
- **效率实验**：消息延迟基准覆盖多种payload大小，对比多个框架和传输方式。
- **可用性实验**：确定性重放实验在混合物理弹球系统上运行100次，统计梯度分布；路径梯度实验区分Retriever与发布/订阅。
- **充分性评估**：
  - **积极方面**：实验覆盖能力、效率、可用性三个维度；消融设计合理；受控实验量化了确定性优势；真实机器人操作验证了可行性。
  - **局限**：真实机器人任务仅两个，且场景相对受限（桌面操作、特定技能集）。消融实验每个特征仅测试一个配置（如计划分块仅为多步vs单步），未搜索最优超参数。缺乏跨平台（多机器人类型）或更复杂场景（动态移动、人机交互）的泛化实验。随机性处理仅依赖记录，未评测实际API非确定性影响。

## 6. 主要结论与发现

- **能力**：Retriever成功支持多速率闭环系统，协调VLM规划（秒级）、VLA技能（2Hz）和200Hz控制，实现长时域操作（香料搜索73%进度，袋检索100%进度）。
- **效率**：异步执行优于阻塞实现，计划分块减少空闲时间；Retriever的运行时开销较低，与Dora相当，优于ROS 2 Python/C++跨进程基线。
- **可用性**：显式时钟和同步策略使得日志驱动的确定性重放可行；在混合物理系统中，事件时间语义产生唯一一致路径梯度，而发布/订阅因调度抖动产生多样梯度。
- **低层策略瓶颈**：任务瓶颈始终是低层技能策略，而高层推理和技能切换可靠。
- **可移植性**：编程模型分离图定义与执行后端，支持调试时单步运行、部署时异步运行。

## 7. 优点

- **全栈解决方案**：从形式化模型到编程语言再到运行时，统一覆盖算法和系统层。
- **显式时序语义**：使时钟、同步、缓冲成为第一公民，消除隐式调度依赖。
- **功能性确定性**：提供可证明的确定性重放，支持调试、验真和可微优化。
- **模块化和可组合**：Flow作为有状态因果函数，通过图连接，不依赖全局状态或回调逻辑。
- **多后端支持**：IR可映射到不同运行时（单进程、Dora等），不绑定特定传输中间件。
- **低开销**：与原生Dora相当的消息延迟，优于ROS 2 Python基线。
- **机器人用例验证**：在真实双机械臂平台上实现闭环层次化操作，展示信念空间规划、监测触发重规划等复杂功能。

## 8. 不足与局限

- **实验覆盖有限**：仅两个桌面操作任务，未在更多样化场景（移动操作、非结构化环境、多人协作）中验证。消融实验仅测试四个特征，未探索超参数影响或特征间交互。
- **低层技能依赖性**：任务成功依赖低层策略（VLA或扩散）的性能，高层框架无法弥补低层失败。训练数据需求仍较大（数百演示），未展示小样本或零样本能力。
- **随机性处理**：对于VLM/VLA中的随机采样，需记录随机种子或原始输出才能实现确定性重放，但论文未讨论API非确定性（如商业模型输出变化）的实际影响。
- **计算延迟假设**：假设慢规划可以重叠执行，但未定量分析不同延迟分布（如VLM推理时间波动）对任务完成时间的影响。同步策略中的滞后和超时处理仅简要提及，未深入实验。
- **可扩展性限制**：尽管支持多后端，但未测试在大规模分布式节点（>数十个Flow）或实时硬实时场景下的表现。
- **用户代码工作量**：仅在消息延迟基准中粗略比较代码行数，未在复杂机器人应用中测量开发成本或代码复用能力。
- **偏差风险**：任务设计可能偏向展示框架优势（如信信念空间搜索），未暴露失败模式或边界情况（如感知彻底失败、规划无限循环）。

（完）
